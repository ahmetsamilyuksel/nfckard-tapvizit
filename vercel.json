{
  "buildCommand": "prisma generate && (prisma db push --accept-data-loss || true) && next build",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/admin/:path*",
      "destination": "/admin/:path*"
    }
  ]
}
